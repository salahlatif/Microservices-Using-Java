server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kulsagar/SpringCloudConfig
          #username: uname
          #password: pass
          search-paths:
          - 'station*'
  security:
    user:
      name: khulja
      password: simsim               